import { memo } from "react";

const Register: React.FC = () => {
  return (
    <div className="w-full min-h-[70vh] text-text-primary-400">
      Register comp
    </div>
  );
};

export default memo(Register);
