export enum CreatorStoreBusinessType {
  /** A single person selling under their own name. */
  INDIVIDUAL = "INDIVIDUAL",
  /** A registered company / establishment with a commercial registration. */
  COMPANY = "COMPANY",
}
